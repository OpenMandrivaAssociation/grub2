diff -Naur grub-2.04/util/grub-mkconfig.in grub-2.04.tpg/util/grub-mkconfig.in
--- grub-2.04/util/grub-mkconfig.in	2020-06-25 13:15:14.950642033 +0000
+++ grub-2.04.tpg/util/grub-mkconfig.in	2020-06-25 13:12:05.459655200 +0000
@@ -176,6 +176,12 @@
   fi
 fi
 
+if [ "x${GRUB_DISABLE_VENDOR_CONF}" = "x" ] || [ "x${GRUB_DISABLE_VENDOR_CONF}" = "xfalse" ]; then
+  if test -e ${sysconfdir}/default/grub.vendor ; then
+    . ${sysconfdir}/default/grub.vendor
+  fi
+fi
+
 # XXX: should this be deprecated at some point?
 if [ "x${GRUB_TERMINAL}" != "x" ] ; then
   GRUB_TERMINAL_INPUT="${GRUB_TERMINAL}"
@@ -256,6 +262,7 @@
   GRUB_SAVEDEFAULT \
   GRUB_ENABLE_CRYPTODISK \
   GRUB_BADRAM \
+  GRUB_DISABLE_VENDOR_CONF \
   GRUB_OS_PROBER_SKIP_LIST \
   GRUB_DISABLE_SUBMENU \
   GRUB_DEFAULT_DTB \
